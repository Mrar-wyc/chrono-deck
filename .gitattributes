# 仓库内统一以 LF 存储。
# Windows 上 git 的 core.autocrlf 会在检出时转成 CRLF，看起来无害——
# 但 shell 脚本带 CRLF 到 Linux 上会直接报 bad interpreter，CI 当场就废，
# 而且只有在别人 clone 之后才会暴露。这条保证仓库里始终是 LF。
* text=auto eol=lf

# 例外：Windows 批处理文件必须是 CRLF
*.bat text eol=crlf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.jar binary
*.apk binary
*.keystore binary

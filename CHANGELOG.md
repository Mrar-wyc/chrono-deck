# 更新日志

## v0.2.1 — 2026-09-17

一次全面体检。收口 M1 之前把「死代码 / 边界与错误路径 / 文档求真」三条线各查了一遍，
找出 4 个真 bug、6 处健壮性缺口、约 10 处死代码与 20 余处文档失实。

### 修复

- **敌人的「自身行动值提前」从未生效过。** `advance()` 在调用 `runEnemyTurn` 之前就把敌人当前
  的行动条目从轴上摘掉了，而重新排入要等到结算完这一招 —— 于是效果里的 `selfAv` 找不到可挪动
  的条目，**静默失效**。抢拍体的「加速 −30」与失序使的「倒拨 −40」一直是空转的，可它们的招式
  轮转表还明明白白展示给玩家。改成与玩家回合对称的顺序：**先排下一次行动，再结算这一招**
- **阵亡的敌人在时序轴上留下幽灵条目。** `view()` 过滤了 `enemies` 却没过滤 `timeline`，
  玩家会在一具尸体的位置看到它的下一步行动；若那一击结束了战斗，这条还会一直留在结算浮层背后
- **倍速按钮的高亮永远失效。** `data-sp` 是从 `textContent`（`"1×"`）取的，`Number("1×")` 是
  `NaN`，比较恒为假 —— 点任何倍速都会把三个按钮的高亮全清掉
- **贡献路径两处阻断。** CONTRIBUTING 的 `defineCard` 示例 `10/2 = 5` 低于付费即时刻印的上限 7，
  照抄必挂；而且 `expect(ALL_CARDS.length).toBe(30)` 是精确计数，**加第 31 张必挂** ——
  等于否定了「加一张刻印只改一个文件」这个对外承诺。计数断言放宽为「不少于」
- **输入校验缺失。** `createBattle` 此前只校验敌人与牌组非空，现在还会拒绝非正的步频/稳定度、
  非法的时能与手牌上限、以及重复的刻印 uid 与非正的挂刻 delay（后者会让时钟倒流）
- **`MAX_STEPS` 从静默 `break` 改为抛错。** 静默 break 会留下一个更糟的状态：界面看起来一切正常，
  但每次出牌都被「现在不是你的回合」弹回来，时钟永远停在那一格
- **时间轴操作的下限统一为 `now + 1`。** 否则一个「招式 av 很小、selfAv 很大」的敌人会在同一个
  行动值上反复出手，时钟再不前进。此前 `cancel` 已经是这么处理的，现在它是一条有名字的规则
- **界面三处**：选择目标时无法用「再点一次」取消（牌一变得不可负担，点击处理器整个被摘掉）；
  `跳过` 在播放之外点击会 latch 给下一次播放，且跳过时整段回放挤在同一个任务里执行、
  一次绘制都没有（中端安卓上可见卡死）；`applyEvent` 用 `default: break` 收尾，导致
  `draw`/`energy`/`turnEnd` 三类事件一直是「有等待、无反馈」—— 现在改成穷尽 switch，
  漏一类就是编译错误

### 变更

- **失序使按实测重定数值。** 修好 `selfAv` 之后它的行动频率涨了约 23%，胜率从 18% 一步掉到 0%。
  没有削弱它的节奏（操纵时间轴是它的招牌），而是把三招的伤害整体下调，让这场战斗靠
  「它越来越快」施压。首领胜率现为 23%
- 时序轴加了五行上限：约 16 条同刻标签会去挤下面装着「结束回合」的那一条，把它推出固定 720px
  的舞台。超出部分合并成一条「另有 N 项」的汇总块，不丢信息
- 删掉约十处死代码：`EMPTY_BUFFS`、四个没人调度的时序轴函数、`dom.clear`、
  `DamageKind` 与伤害事件的 `kind` 字段（只发 `attack` 且无人读）、只写不读的 `UnitState.nextAt`、
  从未被读取的 `EnemyView.moves[].id/name` 与 `EnemyDef.flavor`、以及不在任何里程碑里的 `CardDef.upgraded`
- `vitest` 3.2.7 → 5.0.1，`npm audit` 归零。升级暴露了一个隐藏的脆弱点：此前 `process` 与
  `node:fs` 能通过类型检查是搭了 vitest 传递依赖的便车，现在 `@types/node` 显式声明，
  并用守卫测试守住「src 不得引用 Node API」这条边界

### 测试

202 个（新增 19 条），覆盖上面每一处修复：敌人 `selfAv` 真的产生 shift 事件、阵亡者的条目
不出现在轴上、倍速高亮跟着切换、输入校验的每一条、时序轴行数上限、以及一条「招式 av 很小而
selfAv 很大的敌人也不会把时钟卡住」的极端场景。

### 实测难度曲线

| 档位 | 遭遇 | 胜率 |
|---|---|---|
| 单只普通怪（教学局） | 碎屑体 100% / 迟滞体 100% / 抢拍体 98% / 锈蚀体 100% | 98% – 100% |
| 群战普通怪 | 碎屑双生 | 100%（4.3 回合） |
| 精英 | 锈蚀与碎屑 | 40% |
| 首领 | 失序使 | 23% |

---

## v0.2.0 — 2026-09-17

M1 战斗核心。**现在可以真的打一场了**：时序调度器、事件流引擎、时序轴界面、30 张刻印。

### 新增

- **时序轴战斗**。牌不即时生效，而是排进一条公开的时间轴，在若干行动值之后引爆。同一行动值上的结算顺序是「挂刻 → 玩家 → 敌人」，这条规则是全局最重要的设计，也是校正类牌存在的理由
- **30 张刻印**：即时 9 / 挂刻 8 / 校正 7（另有 6 张起始刻印横跨三类）。挂刻牌的伤害/时能严格高于即时刻印，延迟就是它的价格
- **5 种畸变体 + 7 种遭遇**：碎屑体 / 迟滞体 / 抢拍体 / 锈蚀体 / 首领失序使，覆盖单只、群战、精英、首领四档难度
- **时序轴界面**：横向刻度 + 按行动值定位的彩色标签，同一时刻的条目自动分行避让。这一条是整局全部判断的依据
- **战斗界面**：持久 DOM + 事件回放动画（抽牌、伤害飘字、格挡、时序推进、挂刻引爆）。带 1×/2×/4× 倍速与跳过
- **敌人招式轮转表**显示在敌人卡上，下一招高亮 —— 出招顺序完全确定，把它交给玩家，「时序是一道可解的排序题」才成立
- **筹备界面**加上遭遇选择与选牌，三者（种子 / 遭遇 / 配牌）合起来决定一场完全可复现的战斗
- **机器人策略与平衡回归**：40 局固定种子给出可比较的胜率，按遭遇分档设区间
- **逐回合诊断**（`DIAG=1`）用于人工看数值，默认安静

### 实测难度曲线

（v0.2.0 当时的数字。首领在 v0.2.1 修好 `selfAv` 之后重定为 23%）

| 档位 | 遭遇 | 胜率 |
|---|---|---|
| 单只普通怪（教学局） | 碎屑体 / 迟滞体 / 抢拍体 / 锈蚀体 | 100% |
| 群战普通怪 | 碎屑双生 | 100% |
| 精英 | 锈蚀与碎屑 | 40% |
| 首领 | 失序使 | 18% |

### 测试

176 个（v0.2.1 的体检后为 202 个），2.9 秒跑完。新增引擎规则（排序、挂刻引爆时点、校正、伤害公式、抽牌循环、终局）、确定性（同种子同操作逐条一致、动作回放完全复现）、时序轴布局（比例定位、分行避让）、平衡分档、200 局 soak 不变量、战斗界面冒烟。

### 从实测里学到的三件事

数值不是靠手感调的，是靠胜率数据调的。三个坑都留在了内容文件的注释里：

- **纯操作时间轴的招式会让难度塌掉**。首领原本只有一招带伤害，实测胜率 98%，比精英还容易打
- **永久生效的易伤是乘算放大器**。锈蚀体的易伤每两回合刷新、每回合只衰减一层，等于整场 ×1.5；有它在的双怪组合伤害翻倍，精英胜率从 40% 掉到 0%
- **双怪不能是双强**。玩家的每回合格挡是有限定额，两只满编怪一回合打出的量越过上限时，胜率会从 100% 一步跌到 0%，没有中间档。群战档因此需要一种弱畸变体

还有一条来自截图验收：敌人与手牌之间空出了一条约 170px 的死区（`flex: 1` 的容器吃了剩余高度，里面的卡片却没有撑开）。修法是让卡片撑满，并用**有用的信息**填进去 —— 敌人招式轮转表与自己已挂出未引爆的刻印清单。

---

## v0.1.0 — 2026-09-17

M0 骨架。这一版没有可玩的战斗，交付的是后续所有内容都建立其上的地基。

### 新增

- **种子随机数系统**：mulberry32 + FNV-1a 种子折叠 + 派生子流。**全项目零全局随机源**，由守卫测试扫描源码强制。换来可回放、可复现、种子可分享、平衡测量无随机噪声
- **引擎类型契约**：行动值换算（步频 100 走 100 行动值）、效果词汇表、时序轴条目、`BattleEvent` 全套联合类型。引擎与界面唯一的接口，`engine/types.ts` 自身不依赖任何模块
- **内容层**：刻印、畸变体、遭遇的数据表，以及一个会自动覆盖新增内容的内容校验器
- **界面**：标题、筹备（种子生成 / 复制 / 手输 / 重掷 + 完整卡表）、关于、崩溃（显示真实错误并保留玩家数据）
- **舞台**：固定逻辑尺寸 1440×720 等比缩放，竖屏给出横屏提示
- **流水线**：CI 质量门、Android Debug APK 构建与打 tag 自动发 Release、GitHub Pages 部署
- **文档**：双语 README、CONTRIBUTING、卡牌设计指南、开发进度与设计决定记录、仓库内开发技能

### 这一版刻意避开的三个坑

它们在同规模项目上都是高代价问题，适合在骨架阶段一次性解决：

- **内容查询不抛错**。未知 id 返回 `undefined`，界面用占位卡面顶上。让查询抛错会把防御代码散布到存档加载、回放、开发期手改状态的每一处，总量远超省下的那几行
- **渲染失败不删存档**。捕获异常后显示可读错误并保留玩家数据。白屏且控制台没有线索是排查成本最高的失败，而删掉别人的进度是用一个更大的问题去掩盖一个小问题
- **入口模块可重复执行**。`main.ts` 先清空 `#app` 再建舞台。否则 HMR 重新执行模块时会叠出第二个舞台，`querySelector` 拿到的是上一个，整个页面白屏且没有任何报错——这个是在截图验收时实际抓到的
